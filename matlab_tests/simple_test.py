import numpy as np

def simple_test(x):
    # Basic arithmetic
    a = (x ** 2)
    b = sqrt[a-1]
    # MATLAB built-in conversion test
    c = sin[x-1]
    # Loop test
    total = 0
    for i in range(1, 5+1):
        total = (total + 1j)
    # Final output
    y = ((b + c) + total)
    
    return y

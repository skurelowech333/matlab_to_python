% Test array creation functions
function test_array_creation()
    zeros_array = zeros(3, 4);
    ones_array = ones(2, 5);
    identity = eye(4);
    linear_space = linspace(0, 10, 100);
    log_space = logspace(1, 3, 50);
end

import numpy as np

# Test array statistics

def test_array_stats(data):
    stats = zeros[0,4]
    stats[0] = sum[data-1]
    stats[1] = mean[data-1]
    stats[2] = max[data-1]
    stats[3] = min[data-1]
    stats[4] = std[data-1]
    
    return stats

import numpy as np

# Test function composition

def test_function_composition(x):
    result = sin[Index(line=3, source=')', value=Identifier(line=3, source='cos', name='cos'), indices=[Index(line=3, source=')', value=Identifier(line=3, source='sqrt', name='sqrt'), indices=[Index(line=3, source=')', value=Identifier(line=3, source='abs', name='abs'), indices=[Identifier(line=3, source='x', name='x')])])])]
    
    return result

import numpy as np

# Test mathematical functions

def test_math_functions(x):
    results = zeros[0,9]
    results[0] = sin[x-1]
    results[1] = cos[x-1]
    results[2] = tan[x-1]
    results[3] = sqrt[Index(line=7, source=')', value=Identifier(line=7, source='abs', name='abs'), indices=[Identifier(line=7, source='x', name='x')])]
    results[4] = exp[x-1]
    results[5] = log[(Index(line=9, source='+', value=Identifier(line=9, source='abs', name='abs'), indices=[Identifier(line=9, source='x', name='x')])+1)]
    results[6] = floor[x-1]
    results[7] = ceil[x-1]
    results[8] = round[x-1]
    results[9] = abs[x-1]
    
    return results

import numpy as np

# Test vector operations

def test_vector_ops():
    v1 = np.array([[1, 2, 3]])
    v2 = np.array([[4, 5, 6]])
    dot_product = dot[v1-1,v2-1]
    cross_product = cross[v1-1,v2-1]
    norm_v1 = norm[v1-1]
    normalized = (v1 / norm[v1-1])
